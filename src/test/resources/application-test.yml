spring:
  datasource:
    url: jdbc:h2:mem:omo-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  data:
    redis:
      host: localhost
      port: 6379

  mail:
    host: localhost
    port: 3025
    username: test
    password: test

jwt:
  secret-key: YWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWE=
  access-token-expiration: 1h
  refresh-token-expiration: 7d

cors:
  allowed-origins: http://localhost:3000
